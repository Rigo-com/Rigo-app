import assert from "node:assert/strict";import {analyzeDebugSnapshot} from "../js/admin/admin-agent/subagents/debug-agent/debug-agent.js";
const result=analyzeDebugSnapshot({diagnostics:{errors:[{}],warnings:[{},{}],critical:[],healthScore:70},runtime:{runtimeErrors:[{}],promiseRejections:[{}],crashes:[]},syntax:{errors:[{}]},circular:{circularFound:2},dependency:{missing:[{}],invalid:[]},system:{running:true}});assert.equal(result.errors,4);assert.equal(result.warnings,3);assert.equal(result.critical,0);assert.equal(result.circularDependencies,2);assert.equal(result.healthScore,70);assert.equal(result.healthy,false);console.log("Admin Debug Agent tests passed");
