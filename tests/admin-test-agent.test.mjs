import assert from "node:assert/strict";import {summarizeTests} from "../js/admin/admin-agent/subagents/test-agent/test-agent.js";import {normalizeRuns} from "../api/admin-tests.js";const runs=normalizeRuns([{id:1,name:"Admin",status:"completed",conclusion:"success",head_branch:"ai-bootstrap-wiring"},{id:2,name:"API",status:"completed",conclusion:"failure",head_branch:"ai-bootstrap-wiring"},{id:3,name:"Core",status:"in_progress",conclusion:null,head_branch:"ai-bootstrap-wiring"}]);const summary=summarizeTests({branch:"ai-bootstrap-wiring",runs});assert.equal(summary.total,3);assert.equal(summary.passed,1);assert.equal(summary.failed,1);assert.equal(summary.running,1);assert.equal(summary.failures[0].name,"API");console.log("Admin Test Agent tests passed");
