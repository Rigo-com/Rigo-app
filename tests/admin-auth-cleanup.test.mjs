import assert from "node:assert/strict";import {readFile} from "node:fs/promises";const provider=await readFile(new URL("../js/admin/admin-agent/subagents/project-agent/providers/github-provider.js",import.meta.url),"utf8");const admin=await readFile(new URL("../js/admin/admin-agent/admin-agent.js",import.meta.url),"utf8");assert.equal(provider.includes("/api/admin-login"),false);assert.equal(provider.includes("authenticate,"),false);assert.equal(admin.includes("login admin <secret>"),false);assert.equal(admin.includes("handleLoginCommand"),false);console.log("Admin legacy authentication cleanup tests passed");
