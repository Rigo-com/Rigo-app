import Memory from "../../../memory/index.js";
import {esc,number,sharedStyles,feedback,empty} from "./control-page-utils.js";
const state={container:null,loading:false,error:null,notice:null,status:null,updatedAt:null};
function model(){const value=state.status||{},memory=value.memory||{},cloud=value.cloud||{},scheduler=value.scheduler||{},diagnostics=memory.diagnostics||{};return{initialized:Boolean(memory.initialized),healthy:memory.healthy!==false,phase:memory.currentState||"idle",memories:number(memory.memories),context:number(memory.context),summaries:number(memory.summaries),indexes:number(memory.indexes),created:number(diagnostics.created),searches:number(diagnostics.searches),failures:number(diagnostics.failures),cloud:Boolean(cloud.connected||cloud.configured||scheduler.cloudProviderConfigured),tasks:[scheduler.cleanup,scheduler.summary,scheduler.indexing].filter(Boolean).length};}
function render(){if(!state.container)return false;const data=model();state.container.innerHTML=`<style>${sharedStyles}.memory-hero{display:grid;grid-template-columns:130px 1fr;align-items:center;gap:18px;margin-bottom:13px}.memory-orb{display:grid;place-items:center;width:118px;height:118px;border:1px solid #166b58;border-radius:50%;background:radial-gradient(circle,#0e3b30,#071322 67%);box-shadow:0 0 30px #10b98122}.memory-orb strong{font-size:32px}.memory-orb small{display:block;color:#5ee8b5;text-align:center}.memory-facts{display:grid;grid-template-columns:repeat(2,1fr);gap:8px}.memory-fact{padding:11px;border-radius:11px;background:#091729}.memory-fact strong,.memory-fact span{display:block}.memory-fact span{margin-top:4px;color:#738198;font-size:10px}@media(max-width:760px){.memory-hero{grid-template-columns:100px 1fr}.memory-orb{width:92px;height:92px}.memory-orb strong{font-size:25px}}</style><section class="control-page"><header class="control-head"><div><h1>Memory</h1><p>Live memory health, activity, indexing, and storage.</p></div><div class="control-live"><i></i>${data.initialized?'ONLINE':'STANDBY'}</div></header>${feedback(state)}<section class="control-card memory-hero"><div class="memory-orb"><div><strong>${data.memories}</strong><small>Memories</small></div></div><div><h2>${data.healthy?'Memory Healthy':'Memory Needs Attention'}</h2><p class="control-sub">The RIGO memory subsystem is ${data.initialized?'initialized and ready':'available but not initialized yet'}. Current state: ${esc(data.phase)}.</p><div class="memory-facts"><div class="memory-fact"><strong>${data.tasks}/3</strong><span>MAINTENANCE TASKS</span></div><div class="memory-fact"><strong>${data.cloud?'Ready':'Local'}</strong><span>CLOUD SYNC</span></div></div></div></section><div class="control-actions"><button class="primary" data-memory="refresh" ${state.loading?'disabled':''}>${state.loading?'Refreshing…':'Refresh Status'}</button><button data-memory="maintenance" ${state.loading?'disabled':''}>Run Maintenance</button><button data-memory="open">Open Memory Manager</button></div><div class="control-metrics"><div class="control-metric"><strong>${data.memories}</strong><span>Memories</span></div><div class="control-metric"><strong>${data.context}</strong><span>Context items</span></div><div class="control-metric"><strong>${data.summaries}</strong><span>Summaries</span></div><div class="control-metric"><strong>${data.indexes}</strong><span>Indexes</span></div></div><section class="control-card"><div class="control-title"><h2>Memory Activity</h2><span>${state.updatedAt?`Updated ${new Date(state.updatedAt).toLocaleTimeString()}`:'Waiting for refresh'}</span></div>${state.status?`<div class="control-list"><div class="control-row"><i>＋</i><div><strong>${data.created} memories created</strong><small>Lifetime creation activity</small></div></div><div class="control-row"><i>⌕</i><div><strong>${data.searches} searches completed</strong><small>Memory retrieval activity</small></div></div><div class="control-row"><i>${data.failures?'!':'✓'}</i><div><strong>${data.failures} recorded failures</strong><small>${data.failures?'Review memory diagnostics':'No memory failures detected'}</small></div></div></div>`:empty("◉","Memory status not loaded","Refresh status to inspect the memory subsystem.")}</section></section>`;state.container.querySelector('[data-memory="refresh"]')?.addEventListener("click",()=>refresh());state.container.querySelector('[data-memory="maintenance"]')?.addEventListener("click",()=>maintenance());state.container.querySelector('[data-memory="open"]')?.addEventListener("click",()=>{location.href="./memory.html";});return true;}
async function refresh(){if(state.loading)return false;state.loading=true;state.error=null;state.notice="Refreshing live memory status…";render();try{state.status=Memory.snapshot();state.updatedAt=Date.now();state.notice="Memory status refreshed.";return true;}catch(error){state.error=error?.message||String(error);state.notice=null;return false;}finally{state.loading=false;render();}}
async function maintenance(){if(state.loading)return false;state.loading=true;state.error=null;state.notice="Running memory cleanup and summary maintenance…";render();try{await Memory.initialize();Memory.maintenance();state.status=Memory.snapshot();state.updatedAt=Date.now();state.notice="Memory maintenance completed.";return true;}catch(error){state.error=error?.message||String(error);state.notice=null;return false;}finally{state.loading=false;render();}}
const MemoryPage=Object.freeze({id:"memory",title:"Memory",initialize:()=>true,async mount(container){state.container=container;render();await refresh();return true;},refresh,unmount(){if(state.container)state.container.innerHTML="";state.container=null;return true;},reset(){state.status=null;state.error=null;state.notice=null;return true;},snapshot(){return{loading:state.loading,error:state.error,updatedAt:state.updatedAt,status:model()};}});
export{model,refresh,maintenance,MemoryPage};export default MemoryPage;
